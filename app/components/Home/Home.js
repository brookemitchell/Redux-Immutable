import React, { Proptypes } from 'react'
import {container, title, slogan} from './styles.css'

export default (props) => (
  <div className={container}>
    <p className={title}>{'DUCKER'}</p>
    <p className={slogan}>{'some shit'}</p>
  </div> )
